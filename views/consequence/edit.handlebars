
<section class="conseq-edit">
  <div class="container">
      <div class="row">
          <div class="col">
              <form action="/consequence/edit/{{conseq._id}}?_method=PUT"method="POST" enctype="multipart/form-data">
                  <div class="form-group">
                      <label>Titre de dégat</label>
                      <input class="form-control" type="text" name="title" value="{{conseq.title}}">
                  </div>
                  <div class="form-group">
                      <label>Contenu</label>
                      <textarea class="form-control" rows="5" name="content">{{conseq.content}}</textarea>
                      <script>
                        CKEDITOR.replace( 'content' );
                      </script>
                  </div>
                  <div class="form-group">
                      <label>Auteur</label>
                      <input class="form-control" type="text" name="author" value="{{conseq.author}}">
                  </div>
                  <div class="form-group">
                      <label>image</label>
                      <input class="form-control" type="file" name="image" value="{{conseq.image}}">
                  </div>
                  <button type="submit" class="btn btn-success">Enregistrer</button>
                  <button type="submit" class="btn btn-success">Annuler</button>
              </form>
          </div>
      </div>
  </div>
</section>